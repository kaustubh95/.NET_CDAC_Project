Revision of spring-mv-hibernate with example


vendor deletion

Complete Vendor Management system with
1. 2 way Form binding for --register & update
eg : User registration
Steps in 2 ways form binding
1. In show reg form phase : -- add empty POJO as method argument.
SC -- will create empty POJO instance & auto add it in Model map.

2. Use spring supplied form tag lib for 2 way form binding technique.
eg : 
<sf:form method="post" modelAttribute="vendor">
 <sf:input  path="email" />
3. In process form phase 
In @PostMapping method
Add POJO instance as method argument.
SC --1. Vendor v=new Vendor();
2. invokes matching setters (req param names--path
MUST match POJO prop names.)
3. Injects populated POJO in the req handling method.



For Understanding REST  --MUST go through this

REST  --
Refer to basic video tuts shared.
Read regarding REST --readme  & then REST simplified readme.

refer to spring-restful-sequence

concept & implementation steps for providing Stock as a resource ---
Layers ---StockController--IStockDAO , StockDaoImpl --Stock POJO

1. Create dyn web project , add user lib , web.xml , spring-servlet.xml,<resources> ---db properties n hib persistence xml.

2. Discard --- ViewResolver bean
& view layer(JSP) (If you are converting from existing spring-mvc web application)
3. StockController ---to test Stock server.







